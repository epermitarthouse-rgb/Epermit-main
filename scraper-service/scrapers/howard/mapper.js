"use strict";

/** Real mapping lives in server.js (`mapHowardPipelineToPortalData` adapts Montgomery-shaped pipeline output). */
const MIGRATION_NOTE =
  "Howard portal_data mapping: see server.js mapHowardPipelineToPortalData → mapMontgomeryPipelineToPortalData.";

module.exports = { MIGRATION_NOTE };
