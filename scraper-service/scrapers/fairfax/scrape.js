"use strict";

const accela = require("../../accela-scraper.js");

/**
 * Fairfax-specific waits and extraction live inside scrapeAccelaRecord (accela-scraper.js).
 */

module.exports = {
  scrapeAccelaRecord: accela.scrapeAccelaRecord,
};
