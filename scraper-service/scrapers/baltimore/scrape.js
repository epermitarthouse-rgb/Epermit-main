"use strict";

const accela = require("../../accela-scraper.js");

/**
 * Baltimore-specific waits and extraction live inside scrapeAccelaRecord (accela-scraper.js).
 */

module.exports = {
  scrapeAccelaRecord: accela.scrapeAccelaRecord,
};
