"use strict";

/**
 * Arlington portal_data uses the shared Accela shape from scrapeAccelaRecord (see AccelaProjectView).
 */

module.exports = {
  MIGRATION_NOTE:
    "Arlington shares Accela portal_data with Baltimore/Fairfax patterns; no separate mapper yet.",
};
