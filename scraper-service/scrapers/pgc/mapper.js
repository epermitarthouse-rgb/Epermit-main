"use strict";

/**
 * PGC portal_data mapping (mapPgcPipelineToPortalData, cleanup helpers) remains in server.js.
 */

module.exports = {
  MIGRATION_NOTE:
    "mapPgcPipelineToPortalData and cleanupPgcSuccessLocalArtifacts are trapped in server.js until extracted with regression tests.",
};
