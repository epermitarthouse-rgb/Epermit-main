"use strict";

/**
 * Montgomery portal_data mapping (mapMontgomeryPipelineToPortalData) remains in server.js.
 */

module.exports = {
  MIGRATION_NOTE:
    "mapMontgomeryPipelineToPortalData and Montgomery-specific file-tab helpers are trapped in server.js until extracted with regression tests.",
};
